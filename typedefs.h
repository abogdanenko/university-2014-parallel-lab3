#ifndef TYPEDEFS_H
#define TYPEDEFS_H

#include <complex>
#include <vector>

using std::vector;
using std::complex;

typedef vector<double> Vector;
typedef vector<Vector> Matrix;
typedef Vector::size_type Index;

#endif
